{% extends 'base.html.twig' %}

{% block body %}
<h1>Awale</h1>

<section class="partie-section-joueur">
</section>

<div id="js-awale" data-uuid="{{ game.uuid }}" data-url="{{ url }}" data-player="{{ player.id }}">
</div>
{% endblock %}
