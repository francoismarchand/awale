{% extends 'base.html.twig' %}

{% block body %}
<h1>Awale</h1>

<section class="partie-section-joueur">
</section>

<div id="js-awale" data-uuid="{{ partie.uuid }}" data-url="{{ url }}" data-joueur="{{ joueur }}">
</div>
{% endblock %}
